@main{
    ; res is a bool
    %($bool, res)

    ; x and y are ints
    %($int, x, y)

    ; set x and y to 0
    ~(0, x, y)

    ; set res to result of fn gt
    ~(!(gt, x, y), res)

    ; print res
    !(print, res)
}

@calc{
    %($num, x, y, res)

    ~(!(readline), x)

    
}