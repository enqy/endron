@($block, main)
~({
    !(print, !(div, 1.0, 2.0))
}, main)

!(main, void)

; @($block, calc)
; ~({
;     @($num, x, y, res)
;     @($str, op)
; 
;     ~(!(readline, "First Number: "), x)
;     ~(!(readline, "Second Number: "), y)
;     ~(!(readline, "Operation: "), op)
; 
;     &(op, "+", {
;         !(print, !(add, x, y))
;     })
; }, calc)
; 
;@($block, square)
;~({
;    @($num, in_x)
;    @($num, out)
;
;    ~(!(mul, in_x, in_x), __out)
;}, square)